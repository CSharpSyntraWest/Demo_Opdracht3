<Window x:Class="WpfApp_TESTjsonMVVM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodel="clr-namespace:WpfApp_TESTjsonMVVM.ViewModel"        
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <viewmodel:ProductsViewModel x:Key="ProductsViewModel"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource ProductsViewModel}">
        <StackPanel>
            <TextBox Text="{Binding Path=Name}"/>

            <DataGrid AutoGenerateColumns="True" ItemsSource="{Binding Products}"/>
        </StackPanel>

    </Grid>

</Window>
